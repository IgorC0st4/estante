import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    loadingPage: {
        flex: 1,
        alignContent: 'center',
        alignItems: 'center',
        top: '50%',
    },
    page: {
        padding: 10,
        flex: 1,
    },
    espacamentoInferior: {
        marginBottom: 10
    },
});

export default styles;
